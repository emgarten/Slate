<Project ToolsVersion="15.0">
  <!-- Dependency versions -->
  <PropertyGroup>
  </PropertyGroup>

  <!-- Config -->
  <PropertyGroup>
    <RepositoryName>Slate</RepositoryName>
  </PropertyGroup>

  <!-- Nuspec defaults -->
  <PropertyGroup>
    <Authors>emgarten</Authors>
    <PackageProjectUrl>https://github.com/emgarten/slate</PackageProjectUrl>
    <RepositoryUrl>https://github.com/emgarten/slate</RepositoryUrl>
    <PackageTags>slate</PackageTags>
    <Description>Dotnet helpers for concurrency and exception handling.</Description>
  </PropertyGroup>
</Project>
